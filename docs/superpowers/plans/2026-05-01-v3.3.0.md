# V3.3.0 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**目标：** 修复 #15（平台判断）、#18（DeepSeek 标签 + 工具统计 + footer 动画）、#10（表格超限截断）、#16（多 Profile 支持）

**架构：** 增量最小侵入改法。六个 issue 各自修改独立模块，#16 涉及 config→runner→server 三层。不改动 `/events` 签名和事件协议 schema。向后完全兼容 V3.2.1。

**技术栈：** Python 3.9+ / aiohttp / PyYAML / pytest + pytest-asyncio

---

## 文件结构

| 文件 | 变更 | Issue |
|------|------|-------|
| `install/patcher.py:650-651,702-703` | 修改 2 处各 1 行 | #15 |
| `text.py:5-7,59` | 修改 THINK_TAGS + 新增 count_markdown_tables | #18a #10 |
| `session.py:23,36-37,77` | 新增 _tool_call_count, 改 tool_count, 递增 | #18b |
| `render.py:6-7,61-67,85-92` | 新增 spinner + 表格截断 | #18c #10 |
| `config.py:31,34-55,96` | 新增 profiles schema | #16 |
| `server.py:69-71,119-159,279-313` | 复合 session key + profile 路由 + 健康检查 | #16 |
| `runner.py:33-49,103-127` | 多 profile FeishuBoundary 构建 | #16 |
| `pyproject.toml:7` | 版本号 3.2.0 → 3.3.0 | 全 |
| `config.yaml.example:31-63` | 新增 profiles 示例 | #16 |

---

### Task 1: #15 — COMPLETE_PATCH 增加平台判断

**文件：**
- 修改：`hermes_feishu_card/install/patcher.py:650-651`
- 修改：`hermes_feishu_card/install/patcher.py:702-703`
- 修改：`tests/unit/test_patcher.py`

- [ ] **Step 1: 在 test_patcher.py 中增加平台判断验证用例**

```python
def test_complete_hook_block_contains_platform_check():
    """_render_complete_hook_block 生成的代码包含 source.platform.value == 'feishu'"""
    from hermes_feishu_card.install.patcher import _render_complete_hook_block
    block = "".join(_render_complete_hook_block("    ", "\n"))
    assert "source.platform.value == \"feishu\"" in block
    assert "return None" in block


def test_previous_async_complete_hook_block_contains_platform_check():
    """_render_previous_async_complete_hook_block 生成的代码包含平台判断"""
    from hermes_feishu_card.install.patcher import _render_previous_async_complete_hook_block
    block = "".join(_render_previous_async_complete_hook_block("    ", "\n"))
    assert "source.platform.value == \"feishu\"" in block
    assert "return None" in block


def test_legacy_complete_hook_block_has_no_return_none():
    """_render_legacy_complete_hook_block 没有 return None（fire-and-forget）"""
    from hermes_feishu_card.install.patcher import _render_legacy_complete_hook_block
    block = "".join(_render_legacy_complete_hook_block("    ", "\n"))
    assert "return None" not in block
```

- [ ] **Step 2: 运行测试确认当前失败**

```bash
python -m pytest tests/unit/test_patcher.py::test_complete_hook_block_contains_platform_check -q
```
预期：FAIL — 生成代码中尚无平台判断

- [ ] **Step 3: 修改 _render_complete_hook_block 第 650-651 行**

将 `if _hfc_card_delivered:` 改为 `if _hfc_card_delivered and source.platform.value == "feishu":`

- [ ] **Step 4: 修改 _render_previous_async_complete_hook_block 第 702-703 行**

将 `if _hfc_card_delivered:` 改为 `if _hfc_card_delivered and source.platform.value == "feishu":`

- [ ] **Step 5: 运行测试确认通过**

```bash
python -m pytest tests/unit/test_patcher.py -q
```
预期：PASS

- [ ] **Step 6: Commit**

```bash
git add hermes_feishu_card/install/patcher.py tests/unit/test_patcher.py
git commit -m "fix: add feishu platform check to complete hook (#15)"
```

---

### Task 2: #18a — DeepSeek thinking 标签支持

**文件：**
- 修改：`hermes_feishu_card/text.py:5-7`
- 修改：`tests/unit/test_text.py`

- [ ] **Step 1: 在 test_text.py 中增加 thinking 标签测试**

```python
def test_normalize_removes_thinking_tags():
    from hermes_feishu_card.text import normalize_stream_text
    assert normalize_stream_text("<thinking>推理中</thinking>结果") == "推理中结果"
    assert normalize_stream_text("<THINKING>推理</THINKING>") == "推理"


def test_streaming_normalizer_handles_thinking_split_across_chunks():
    from hermes_feishu_card.text import StreamingTextNormalizer
    normalizer = StreamingTextNormalizer()
    assert normalizer.feed("hello<think") == "hello"
    assert normalizer.feed("ing>world") == "world"


def test_normalize_does_not_remove_plain_think_word():
    from hermes_feishu_card.text import normalize_stream_text
    assert normalize_stream_text("I think so") == "I think so"
    assert normalize_stream_text("I am thinking") == "I am thinking"
```

- [ ] **Step 2: 运行测试确认当前失败**

```bash
python -m pytest tests/unit/test_text.py::test_normalize_removes_thinking_tags -q
```
预期：FAIL

- [ ] **Step 3: 修改 text.py 第 5、7 行**

```python
# 改前：
THINK_TAG_RE = re.compile(r"</?think>", re.IGNORECASE)
THINK_TAGS = ("<think>", "</think>")

# 改后：
THINK_TAG_RE = re.compile(r"</?think>|</?thinking>", re.IGNORECASE)
THINK_TAGS = ("<think>", "</think>", "<thinking>", "</thinking>")
```

- [ ] **Step 4: 运行测试确认通过**

```bash
python -m pytest tests/unit/test_text.py -q
```
预期：PASS

- [ ] **Step 5: Commit**

```bash
git add hermes_feishu_card/text.py tests/unit/test_text.py
git commit -m "feat: support <thinking> tags for DeepSeek reasoning (#18a)"
```

---

### Task 3: #18b — 工具次数正确统计

**文件：**
- 修改：`hermes_feishu_card/session.py:23,36-37,77`
- 修改：`tests/unit/test_session.py`

- [ ] **Step 1: 在 test_session.py 中增加工具计数测试**

```python
def test_tool_count_increments_for_same_tool_id():
    from hermes_feishu_card.session import CardSession
    from hermes_feishu_card.events import SidecarEvent
    session = CardSession(conversation_id="c", message_id="m", chat_id="c")
    for i in range(3):
        event = SidecarEvent(
            schema_version="1", event="tool.updated",
            conversation_id="c", message_id="m", chat_id="c",
            platform="feishu", sequence=i, created_at=0.0,
            data={"tool_id": "web_search", "name": "web_search", "status": "running"},
        )
        session.apply(event)
    assert session.tool_count == 3
    assert len(session.tools) == 1  # tools 字典仍去重


def test_tool_count_increments_for_different_tool_ids():
    from hermes_feishu_card.session import CardSession
    from hermes_feishu_card.events import SidecarEvent
    session = CardSession(conversation_id="c", message_id="m", chat_id="c")
    for i, tid in enumerate(["a", "b", "c"]):
        event = SidecarEvent(
            schema_version="1", event="tool.updated",
            conversation_id="c", message_id="m", chat_id="c",
            platform="feishu", sequence=i, created_at=0.0,
            data={"tool_id": tid, "name": tid, "status": "running"},
        )
        session.apply(event)
    assert session.tool_count == 3
```

- [ ] **Step 2: 运行测试确认当前失败**

```bash
python -m pytest tests/unit/test_session.py::test_tool_count_increments_for_same_tool_id -q
```
预期：FAIL — 当前返回 len(tools)=1

- [ ] **Step 3: 修改 session.py**

在第 23 行 `duration: float = 0.0` 之后新增：
```python
    _tool_call_count: int = field(default=0)
```

将第 36-37 行改为：
```python
    @property
    def tool_count(self) -> int:
        return self._tool_call_count
```

在第 77 行 `self.tools[tool_id] = ToolState(...)` 块末尾增加：
```python
            self._tool_call_count += 1
```

- [ ] **Step 4: 运行测试确认通过**

```bash
python -m pytest tests/unit/test_session.py -q
```
预期：PASS

- [ ] **Step 5: Commit**

```bash
git add hermes_feishu_card/session.py tests/unit/test_session.py
git commit -m "fix: tool_count tracks actual calls not unique count (#18b)"
```

---

### Task 4: #18c — Footer 旋转动画

**文件：**
- 修改：`hermes_feishu_card/render.py:6-7,85-92`（新增 import + spinner + 修改 footer）
- 修改：`tests/unit/test_render.py`

- [ ] **Step 1: 在 test_render.py 中增加 spinner 测试**

```python
import time

def test_spinner_text_changes_over_time():
    from hermes_feishu_card.render import _spinner_text
    frames = set()
    for _ in range(20):
        frames.add(_spinner_text("生成中"))
        time.sleep(0.05)
    assert len(frames) >= 2  # 至少两个不同帧


def test_spinner_text_contains_label():
    from hermes_feishu_card.render import _spinner_text
    assert "处理中" in _spinner_text("处理中")


def test_footer_shows_spinner_not_static_for_thinking():
    from hermes_feishu_card.session import CardSession
    from hermes_feishu_card.render import _render_footer
    session = CardSession(conversation_id="c", message_id="m", chat_id="c")
    session.status = "thinking"
    footer = _render_footer(session)
    assert footer != "生成中"
    assert "生成中" in footer


def test_footer_still_static_for_failed():
    from hermes_feishu_card.session import CardSession
    from hermes_feishu_card.render import _render_footer
    session = CardSession(conversation_id="c", message_id="m", chat_id="c")
    session.status = "failed"
    assert _render_footer(session) == "已停止"
```

- [ ] **Step 2: 运行测试确认当前失败**

```bash
python -m pytest tests/unit/test_render.py::test_spinner_text_changes_over_time -q
```
预期：FAIL — _spinner_text 未定义

- [ ] **Step 3: 修改 render.py**

在第 6 行 `from .text import normalize_stream_text` 之后增加：
```python
import time as _time
```

在第 16 行 `DEFAULT_TITLE = "Hermes Agent"` 之后增加：
```python
_SPINNER_FRAMES = ("⠋", "⠙", "⠹", "⠸", "⠼", "⠴", "⠦", "⠧", "⠇", "⠏")

def _spinner_text(label: str = "生成中") -> str:
    frame = _SPINNER_FRAMES[int(_time.time() * 5) % len(_SPINNER_FRAMES)]
    return f"{frame} {label}"
```

将第 91-92 行 `return "生成中"` 改为 `return _spinner_text("生成中")`

- [ ] **Step 4: 运行测试确认通过**

```bash
python -m pytest tests/unit/test_render.py -q
```
预期：PASS

- [ ] **Step 5: Commit**

```bash
git add hermes_feishu_card/render.py tests/unit/test_render.py
git commit -m "feat: add spinner animation to footer (#18c)"
```

---

### Task 5: #10 — 卡片表格超限截断

**文件：**
- 修改：`hermes_feishu_card/text.py:59`（新增 count_markdown_tables + MAX_CARD_TABLES）
- 修改：`hermes_feishu_card/render.py:61-67`（新增表格截断逻辑）
- 修改：`tests/unit/test_text.py`、`tests/unit/test_render.py`

- [ ] **Step 1: 在 test_text.py 中增加表格计数测试**

```python
def test_count_markdown_tables_zero():
    from hermes_feishu_card.text import count_markdown_tables
    assert count_markdown_tables("hello world") == 0
    assert count_markdown_tables("| name | age |") == 0  # 没有分隔行


def test_count_markdown_tables_normal():
    from hermes_feishu_card.text import count_markdown_tables
    text = "| a | b |\n| --- | --- |\n| 1 | 2 |\n\n| x | y |\n| --- | --- |\n| 3 | 4 |"
    assert count_markdown_tables(text) == 2


def test_count_markdown_tables_seven():
    from hermes_feishu_card.text import count_markdown_tables
    text = "\n\n".join([f"| col |\n| --- |\n| {i} |" for i in range(7)])
    assert count_markdown_tables(text) == 7


def test_max_card_tables_constant():
    from hermes_feishu_card.text import MAX_CARD_TABLES
    assert MAX_CARD_TABLES == 5
```

- [ ] **Step 2: 在 test_render.py 中增加截断测试**

```python
def test_render_card_truncates_tables_over_limit():
    from hermes_feishu_card.session import CardSession
    from hermes_feishu_card.render import render_card
    session = CardSession(conversation_id="c", message_id="m", chat_id="c")
    # 构造 7 个表格的正文
    session.answer_text = "\n\n".join(
        [f"Table {i}\n| col |\n| --- |\n| {i} |" for i in range(7)]
    )
    session.status = "completed"
    card = render_card(session)
    body_text = "".join(
        el.get("content", "") for el in card["body"]["elements"]
        if el.get("tag") == "markdown"
    )
    assert "超出部分已省略" in body_text
```

- [ ] **Step 3: 运行测试确认当前失败**

```bash
python -m pytest tests/unit/test_text.py::test_count_markdown_tables_zero -q
```
预期：FAIL — count_markdown_tables 未定义

- [ ] **Step 4: 在 text.py 末尾新增**

```python
def count_markdown_tables(text: str) -> int:
    return len(re.findall(r'^\|[-: ]+\|', text, re.MULTILINE))

MAX_CARD_TABLES = 5
```

- [ ] **Step 5: 修改 render.py 的 _render_main_content_elements**

将函数改为：
```python
def _render_main_content_elements(main_text: str) -> list:
    from .text import count_markdown_tables, MAX_CARD_TABLES
    table_count = count_markdown_tables(main_text)
    if table_count > MAX_CARD_TABLES:
        matches = list(re.finditer(r'^\|[-: ]+\|', main_text, re.MULTILINE))
        cutoff = matches[MAX_CARD_TABLES - 1].end()
        rest = main_text[cutoff:]
        next_para = re.search(r'\n\n', rest)
        if next_para:
            cutoff += next_para.start()
        main_text = main_text[:cutoff].rstrip() + (
            "\n\n> 内容含超过 5 个表格，超出部分已省略。"
        )
    chunks = _split_text(main_text, MAIN_CONTENT_CHUNK_CHARS)
    elements = []
    for index, chunk in enumerate(chunks):
        element_id = "main_content" if index == 0 else f"main_content_{index}"
        elements.append({"tag": "markdown", "element_id": element_id, "content": chunk})
    return elements
```

并在 render.py 头部增加 `import re as _re`（或直接在函数内使用 re）。

- [ ] **Step 6: 运行测试确认通过**

```bash
python -m pytest tests/unit/test_text.py tests/unit/test_render.py -q
```
预期：PASS

- [ ] **Step 7: Commit**

```bash
git add hermes_feishu_card/text.py hermes_feishu_card/render.py tests/unit/test_text.py tests/unit/test_render.py
git commit -m "fix: truncate tables when exceeding feishu 5-table limit (#10)"
```

---

### Task 6: #16 — 多 Profile 支持（配置层）

**文件：**
- 修改：`hermes_feishu_card/config.py:31,34-55,96`
- 修改：`tests/unit/test_config.py`

- [ ] **Step 1: 在 test_config.py 中增加 profiles 配置测试**

```python
def test_load_config_with_profiles():
    import tempfile, yaml
    from hermes_feishu_card.config import load_config
    profiles_config = {
        "profiles": {
            "default": {
                "feishu": {"app_id": "cli_a", "app_secret": "s_a"},
                "bots": {"default": "default", "items": {}},
                "bindings": {"chats": {}, "fallback_bot": "default"},
            },
            "work": {
                "feishu": {"app_id": "cli_b", "app_secret": "s_b"},
                "bots": {"default": "default", "items": {}},
                "bindings": {"chats": {}, "fallback_bot": "default"},
            },
        }
    }
    with tempfile.NamedTemporaryFile(mode="w", suffix=".yaml", delete=False) as f:
        yaml.dump(profiles_config, f)
        path = f.name
    try:
        config = load_config(path)
        assert "profiles" in config
        assert config["profiles"]["default"]["feishu"]["app_id"] == "cli_a"
        assert config["profiles"]["work"]["feishu"]["app_id"] == "cli_b"
    finally:
        import os; os.unlink(path)


def test_load_config_profiles_overrides_top_level():
    import tempfile, yaml
    from hermes_feishu_card.config import load_config
    config_data = {
        "feishu": {"app_id": "cli_top", "app_secret": "s_top"},
        "profiles": {
            "default": {
                "feishu": {"app_id": "cli_default", "app_secret": "s_default"},
            },
        },
    }
    with tempfile.NamedTemporaryFile(mode="w", suffix=".yaml", delete=False) as f:
        yaml.dump(config_data, f)
        path = f.name
    try:
        config = load_config(path)
        # profiles 存在时忽略顶层 feishu
        assert "profiles" in config
        assert config["profiles"]["default"]["feishu"]["app_id"] == "cli_default"
    finally:
        import os; os.unlink(path)


def test_load_config_no_profiles_uses_top_level():
    import tempfile, yaml
    from hermes_feishu_card.config import load_config
    config_data = {"feishu": {"app_id": "cli_top", "app_secret": "s_top"}}
    with tempfile.NamedTemporaryFile(mode="w", suffix=".yaml", delete=False) as f:
        yaml.dump(config_data, f)
        path = f.name
    try:
        config = load_config(path)
        assert config["feishu"]["app_id"] == "cli_top"
        assert "profiles" not in config or not config["profiles"]
    finally:
        import os; os.unlink(path)
```

- [ ] **Step 2: 运行测试确认当前失败**

```bash
python -m pytest tests/unit/test_config.py::test_load_config_with_profiles -q
```
预期：FAIL — profiles schema 尚未定义

- [ ] **Step 3: 修改 config.py**

在 `DEFAULT_CONFIG` 末尾增加 profiles 默认值（第 31 行附近）：
```python
    "profiles": {},
```

修改 `load_config` 函数，在 `_merge_sections(config, loaded)` 之后、`_apply_env_overrides` 之前增加 profile 展开逻辑：
```python
    profiles = config.get("profiles")
    if isinstance(profiles, dict) and profiles:
        for profile_id, profile_cfg in profiles.items():
            if isinstance(profile_cfg, dict):
                profile_cfg.setdefault("feishu", copy.deepcopy(DEFAULT_CONFIG["feishu"]))
                profile_cfg.setdefault("bots", copy.deepcopy(DEFAULT_CONFIG["bots"]))
                profile_cfg.setdefault("bindings", copy.deepcopy(DEFAULT_CONFIG["bindings"]))
```

`_apply_env_overrides` 中，如果 profiles 存在则跳过顶层 env override（凭据由各 profile 独立管理）。

- [ ] **Step 4: 运行测试确认通过**

```bash
python -m pytest tests/unit/test_config.py -q
```
预期：PASS

- [ ] **Step 5: Commit**

```bash
git add hermes_feishu_card/config.py tests/unit/test_config.py
git commit -m "feat: add profiles config schema for multi-profile support (#16)"
```

---

### Task 7: #16 — 多 Profile 支持（runner 层）

**文件：**
- 修改：`hermes_feishu_card/runner.py:33-49,103-127`
- 修改：`tests/unit/test_runner.py`

- [ ] **Step 1: 在 test_runner.py 中增加多 profile boundary 测试**

```python
def test_build_feishu_boundary_multi_profile():
    from hermes_feishu_card.runner import build_feishu_boundary
    config = {
        "profiles": {
            "default": {
                "feishu": {"app_id": "a", "app_secret": "s1"},
                "bots": {"default": "default", "items": {}},
                "bindings": {"chats": {}, "fallback_bot": "default"},
            },
            "work": {
                "feishu": {"app_id": "b", "app_secret": "s2"},
                "bots": {"default": "default", "items": {}},
                "bindings": {"chats": {}, "fallback_bot": "default"},
            },
        },
        "server": {"host": "127.0.0.1", "port": 8765},
    }
    boundary = build_feishu_boundary(config)
    # 多 profile 返回 dict
    assert isinstance(boundary.client, dict)
    assert set(boundary.client.keys()) == {"default", "work"}
    assert callable(boundary.router)
```

- [ ] **Step 2: 运行测试确认当前失败**

```bash
python -m pytest tests/unit/test_runner.py::test_build_feishu_boundary_multi_profile -q
```

- [ ] **Step 3: 修改 runner.py 的 build_feishu_boundary**

支持多 profile 模式：如果 `config.get("profiles")` 存在且非空，为每个 profile 构建独立的 FeishuClientFactory 和 BotRegistry，返回 `FeishuBoundary(client=dict_of_factories, router=profile_router_fn)`。`profile_router_fn` 是一个接受 profile_id 参数的函数，从事件 data 提取 profile_id，查对应 registry 做 resolve。

- [ ] **Step 4: 运行测试确认通过**

```bash
python -m pytest tests/unit/test_runner.py -q
```

- [ ] **Step 5: Commit**

```bash
git add hermes_feishu_card/runner.py tests/unit/test_runner.py
git commit -m "feat: multi-profile boundary in runner (#16)"
```

---

### Task 8: #16 — 多 Profile 支持（server 层）

**文件：**
- 修改：`hermes_feishu_card/server.py:69-71,119-159,279-313`
- 修改：`tests/integration/test_server.py`
- 修改：`hermes_feishu_card/server.py`（`create_app` 新增 `profiles` 参数）

步长较大，拆分子步骤：

- [ ] **Step 1: session key 复合化**

在 `_apply_event_locked` 中，session key 从 `event.message_id` 改为由 `_session_key(event)` 生成：
- 单 profile（`app[PROFILES_KEY]` 为空或不存在）：`key = event.message_id`
- 多 profile：`profile_id = event.data.get("profile_id", "default")`；`key = f"{profile_id}:{event.message_id}"`

- [ ] **Step 2: profile 路由**

新增 `PROFILES_KEY` app key。在 `_resolve_route` 中，根据当前事件的 `profile_id` 从 `app[PROFILES_KEY]` 取出对应的 `FeishuBoundary` 进行路由。在 `_client_for_bot` 中同理。

- [ ] **Step 3: 健康检查增强**

`_health` 响应增加 `profiles` 字段：遍历所有 profile 下的 session 分组，返回每个 profile 的 `active_sessions` 和 `metrics` 快照。单 profile 模式下不返回此字段（兼容）。

- [ ] **Step 4: 在 test_server.py 中增加多 profile 集成测试**

```python
@pytest.mark.asyncio
async def test_profile_isolation_sessions():
    """不同 profile 的消息到达不同 session"""
    # 创建含两个 profile 的 app
    # 发送 default profile 的 message.started
    # 发送 work profile 的 message.started
    # 验证两个 session 独立存在
```

- [ ] **Step 5: 运行全量测试确认无回归**

```bash
python -m pytest -q
```

- [ ] **Step 6: Commit**

```bash
git add hermes_feishu_card/server.py tests/integration/test_server.py
git commit -m "feat: multi-profile session routing and health in server (#16)"
```

---

### Task 9: 版本号 + 配置示例 + 最终验证

**文件：**
- 修改：`pyproject.toml:7`
- 修改：`config.yaml.example`
- 新建：`CHANGELOG.md`（追加 V3.3.0 条目）

- [ ] **Step 1: 更新 pyproject.toml 版本号**

```python
version = "3.3.0"
```

- [ ] **Step 2: 更新 config.yaml.example**

在现有内容末尾增加 profiles 配置注释示例。

- [ ] **Step 3: 更新 CHANGELOG.md**

追加 V3.3.0 条目，列出全部 fix/feat。

- [ ] **Step 4: 全量测试最终验证**

```bash
python -m pytest -q
```
预期：所有测试 PASS，无回归

- [ ] **Step 5: Commit**

```bash
git add pyproject.toml config.yaml.example CHANGELOG.md
git commit -m "chore: bump version to 3.3.0, update config example and changelog"
```

---

## 自我审查

| 检查项 | 结果 |
|--------|------|
| **Spec 覆盖** | Task 1→#15, Task 2→#18a, Task 3→#18b, Task 4→#18c, Task 5→#10, Tasks 6-8→#16。全部覆盖。 |
| **占位符** | 无 TBD/TODO。每一步包含确切代码或明确指示。 |
| **类型一致性** | `_tool_call_count` 在 session.py 定以后，后续 render.py 通过 `session.tool_count` 使用，接口不变。`profiles` 在 config→runner→server 三层传递类型一致。 |
| **向后兼容** | 所有改动在单 profile 模式下行为完全不变。 |
